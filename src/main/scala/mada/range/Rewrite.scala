
package mada.range

/*

object Rewrite {
    def apply[E](r: Range[E]): Range[E] = r match {
        case TransformRange(TransformRange(s, g), f) => apply(TransformRange(s, g compose f))
        case _ => apply(r.base)
    }
}

*/
